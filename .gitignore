# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Data — large files, not versioned
data/
*.laz
*.las
*.ply
*.pcd

# Outputs — images, large CSVs
*.png
*.jpg
outputs/ood_energy_dist.csv

# OOD basket — runtime-generated embeddings (not versioned)
outputs/ood_basket/

# Notebooks
notebooks/.ipynb_checkpoints/
*.ipynb_checkpoints/

# Model weights — versioned (PointNet2Mini = ~388KB, fine for git)
# *.pt  ← intentionally NOT excluded (small en